
public class cheatsheet {
    public static void main(String[] args) {
    }
        /*
          Predicate<Input> is like IF  (true / false)
      Function<Input , Output> --> function
      Consumer<Input> --> like foreach etc.
      Supplier<Output> --> like producer. gives result.
      IntFunction<Input> --> output is primitive int.







 Collections.reverse();


Stack --> push() , pop()

Queue --> offer() , poll()

private static boolean isInBounds(int r, int c) {
        return  r>=0 && r <15 && c >=0 && c <15;
    }

     */


    //if empty return none ,else print delimiters by ','
   // public static String formatArrayDeque(ArrayDeque<Integer> deque){
   //  if (deque.isEmpty()){
   //   return "none";
   //  }
   //  return deque.stream().map(String::valueOf)
   //          .collect(Collectors.joining(", "));
   // }


 }
